import React from 'react';
import ReactDOM from 'react-dom/client';
import BlameWardGame from './blame-ward-game';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BlameWardGame />
  </React.StrictMode>
);
